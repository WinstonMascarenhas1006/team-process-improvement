# Data layout

Case bundles live in `cases/<id>/case.json`. The old flat files (`impediments.json`, etc.) are kept for reference but the tools read from the case folders now.

Start the dashboard or pass `--case` on the CLI scripts.
